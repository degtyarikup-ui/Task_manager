# 📱 Инструкция по развертыванию Task Manager в Telegram

## ✅ Что уже сделано автоматически

1. ✅ Интегрирован Telegram Web App SDK
2. ✅ Настроена автоматическая синхронизация темы с Telegram
3. ✅ Адаптирован viewport для мобильных устройств
4. ✅ Настроена система переводов (русский/английский)
5. ✅ Оптимизирована UI для использования в Telegram

---

## 🚀 Шаг 1: Сборка production-версии

Выполните в терминале:

```bash
npm run build
```

Это создаст папку `dist` с готовым приложением.

---

## 🌐 Шаг 2: Деплой приложения на хостинг

### Вариант А: GitHub Pages (Рекомендуется - БЕСПЛАТНО)

1. **Создай репозиторий на GitHub**:
   - Зайди на https://github.com/new
   - Назови репозиторий например: `task-manager-tg`
   - Сделай его публичным
   - Нажми "Create repository"

2. **Загрузи код в репозиторий**:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/task-manager-tg.git
   git push -u origin main
   ```

3. **Настрой GitHub Pages**:
   - Зайди в Settings репозитория
   - Слева выбери "Pages"
   - В "Source" выбери "GitHub Actions"
   
4. **Создай файл для автодеплоя**:
   Создай файл `.github/workflows/deploy.yml` с содержимым:
   
   ```yaml
   name: Deploy to GitHub Pages

   on:
     push:
       branches: [ main ]

   jobs:
     build-and-deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         
         - name: Setup Node.js
           uses: actions/setup-node@v3
           with:
             node-version: '18'
             
         - name: Install dependencies
           run: npm ci
           
         - name: Build
           run: npm run build
           
         - name: Deploy to GitHub Pages
           uses: peaceiris/actions-gh-pages@v3
           with:
             github_token: ${{ secrets.GITHUB_TOKEN }}
             publish_dir: ./dist
   ```

5. **После деплоя твой сайт будет доступен по адресу**:
   ```
   https://ВАШ_USERNAME.github.io/task-manager-tg/
   ```

### Вариант Б: Vercel (Еще проще - БЕСПЛАТНО)

1. Зайди на https://vercel.com
2. Подключи свой GitHub аккаунт
3. Нажми "New Project"
4. Выбери свой репозиторий `task-manager-tg`
5. Vercel автоматически определит настройки Vite
6. Нажми "Deploy"
7. Готово! Vercel даст тебе ссылку типа: `https://task-manager-tg.vercel.app`

---

## 🤖 Шаг 3: Создай Telegram бота

1. **Открой Telegram и найди @BotFather**

2. **Создай нового бота**:
   - Отправь: `/newbot`
   - Введи имя бота (например: "Task Manager")
   - Введи username бота (должен заканчиваться на bot, например: `my_task_manager_bot`)
   
3. **Сохрани токен бота** - он понадобится позже (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## 🔗 Шаг 4: Подключи Web App к боту

1. **Создай Web App**:
   В разговоре с @BotFather отправь:
   ```
   /newapp
   ```

2. **Выбери своего бота** из списка

3. **Заполни данные:**
   - **Title**: Task Manager
   - **Description**: Управление задачами и клиентами
   - **Photo**: Загрузи иконку приложения (512x512px)
   - **Demo GIF/Video**: Можешь пропустить (отправь /empty)
   - **Web App URL**: Вставь ссылку на твое приложение
     - Для GitHub Pages: `https://ВАШ_USERNAME.github.io/task-manager-tg/`
     - Для Vercel: `https://task-manager-tg.vercel.app`
   - **Short name**: task_manager (используется в inline-кнопках)

4. **Готово!** BotFather создаст Web App

---

## 🎮 Шаг 5: Создай кнопку для запуска приложения

1. **Установи меню бота**:
   Отправь @BotFather:
   ```
   /mybots
   ```
   Выбери своего бота → **Edit Bot** → **Edit Commands**

2. **Добавь команду**:
   ```
   start - Открыть Task Manager
   ```

3. **Настрой клавиатуру с Web App кнопкой**:
   Для этого тебе нужно написать скрипт для бота. Можешь использовать простой Python скрипт:

   ```python
   from telegram import Bot, WebAppInfo, ReplyKeyboardMarkup, KeyboardButton

   TOKEN = "ВАШ_ТОКЕН_БОТА"
   bot = Bot(token=TOKEN)

   # URL твоего Web App
   WEB_APP_URL = "https://ВАШ_USERNAME.github.io/task-manager-tg/"

   # Создаем кнопку с Web App
   keyboard = ReplyKeyboardMarkup([
       [KeyboardButton("📋 Открыть Task Manager", web_app=WebAppInfo(url=WEB_APP_URL))]
   ], resize_keyboard=True)

   # Отправь это себе для теста (замени YOUR_TELEGRAM_ID на свой ID)
   bot.send_message(
       chat_id=YOUR_TELEGRAM_ID,
       text="Нажми кнопку ниже, чтобы открыть приложение:",
       reply_markup=keyboard
   )
   ```

---

## 🎯 Шаг 6: Тестирование

1. Найди своего бота в Telegram
2. Напиши `/start`
3. Нажми на кнопку "📋 Открыть Task Manager"
4. Приложение откроется в полноэкранном режиме!

---

## 🔧 Важные настройки для твоего vite.config.ts

Добавь `base` для правильной работы на GitHub Pages:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: './', // Для GitHub Pages используй: '/task-manager-tg/'
})
```

---

## 💡 Дополнительные возможности

### Inline Mode
Чтобы пользователи могли отправлять твоё приложение в другие чаты:

1. В @BotFather отправь: `/setinline`
2. Выбери бота
3. Введи текст подсказки (например: "Управл задачами")

### Кнопка "Меню"
Добавь кнопку Menu в Telegram:

1. Отправь @BotFather: `/mybots`
2. Выбери бота → **Bot Settings** → **Menu Button**
3. Введи URL твоего Web App

---

## 📝 Если что-то не работает

1. **Проверь URL**: Открой его в браузере - должно работать
2. **Проверь HTTPS**: Telegram требует HTTPS (GitHub Pages и Vercel дают его автоматически)
3. **Проверь консоль**: Открой DevTools в браузере и посмотри ошибки
4. **Telegram кеш**: Иногда Telegram кеширует старую версию - попробуй переоткрыть бота

---

## 🎨 Что уже адаптировано для Telegram:

- ✅ Viewport настроен для полноэкранного режима
- ✅ Тема автоматически синхронизируется с Telegram
- ✅ Приложение готово к использованию на мобильных устройствах
- ✅ Цветовая схема адаптирована под Telegram
- ✅ Поддержка русского и английского языков

---

## 🚀 Готово!

Теперь у тебя есть Task Manager, который работает внутри Telegram как полноценное приложение!
